@model IEnumerable<User>
<h2 style="text-align: center">User list</h2>
@foreach (var user in Model)
{
    <div>
         <a asp-controller="Home" asp-action="CreatePrivateRoom" asp-route-userId="@user.Id" class="main-nav__link">
             @user.UserName
         </a>
    </div>
}
